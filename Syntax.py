print("Syntax")
# Коментарии начинаются со знака хеш.
# Отступы используются для обозначения блоков кода.
# Для отступа испоьзовать 4 пробела (минимум 1).
# В одном блоке одинаковый отступ.
# Переменные создаются в момент присваивания им значений.
# Переменные могут менять свой тип после присвоения.
# Тип (type) переменной (variable) можно указать с помощью приведения:
# x = str(3)
# Data type of a vaiable can get with the function type()
# print(type(x))
# Имена (name) variables учитывают регистр символов.
# Variable name должны начинаться с буквы или подчеркивания:"_"
# Variable name не должно начинаться с number.
# Variable name может содержать буквы, цифры, подчеркивание.
# Variable name не может быть ключевым словом Python (Python keywords).
# Variable name многословные можно записывать: camelCase, PascalCase, snake_case.
# Программа(Program) - список(list) инструкций (statement).
# Statement заканчивается концом строки.
# ";" может разделять несколько statement в одной строке программы (line).
# Statement1; Statement2; Statement3
# Несколько statrments в одной line без (without) ";" дадут (will give) ошибку (an error).
# Размещать (Put) 1 statement on 1 line.
# Statement: print() выводит аргумент на экран on a new line.
# Текст (text) размещать в кавычках(quotes) "_" | '_'.
# Text without quotes will give an error.
# Чтобы печтать text из разных statement от 1 line использовать (use) параметр (parametr) "end".
# print("text1", end=" ")
# print("text2")
# Цифры (numbers) in quotes (не заключать)  don't put.
# Inside (внутри) function:print() can (можно) do math (делать вычисления).
# print(1 + 1)
# Inside print() can combine (объединять) text and numbers separating (разделяя) them (их) ",".
# Multiline string (многострочный комментарий) между 3-ми quotes:
"""
Text sting1
Text string2
"""

